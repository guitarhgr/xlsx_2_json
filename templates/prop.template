
let resultObj = {};

for (let i = 0; i < mixData.length; i++) {
    const data = mixData[i];

    if (!data) return;

    resultObj[data.id] = [data.id, data.name, data.selftype, data.width, data.height];
}

return resultObj;